#Localizar info. com condição
SELECT <coluna1>, <coluna2>,
FROM <nome_tabela>
WHERE <condição>

#Condição para localizar um ID especifico:
SELECT * FROM <nome_tabela>
WHERE <coluna_id>='1'

#Condição para localizar uma info especifica:
SELECT * FROM <nome_tabela>
WHERE <coluna1> = '<informação>'