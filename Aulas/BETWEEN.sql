/*
BETWEEN - Seleciona os valores dentro de um determinado intervalo
*/

#Utilizando o BETWEEN
SELECT <coluna>
FROM <nome_tabela>
WHERE <coluna> BETWEEN <valor1> AND <valor2>

#Exemplo 
SELECT <coluna>
FROM <nome_tabela>
WHERE <coluna> BETWEEN 10 AND 20