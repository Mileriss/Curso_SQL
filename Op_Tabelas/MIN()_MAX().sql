/*
MIN() - Vai retornar o menor valor da coluna selecionada
MAX() - Vai retornar o maior valor da coluna selecionada
*/

#Utilizando MAX()
SELECT MAX(<coluna>)
FROM <nome_tabela>
WHERE <condição>

#UTILIZANDO MIN()
SELECT MIN(<coluna>)
FROM <nome_tabela>
WHERE <condição>