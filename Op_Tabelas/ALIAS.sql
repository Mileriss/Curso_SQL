/*
ALIAS / AS - É utilizado para nomear temporariamente uma coluna ou tabela
*/

#Utilizando o ALIAS/AS em colunas
SELECT <nome_coluna> AS <outro_nome_coluna>
FROM <nome_tabela>

#Utilizando o ALIAS/AS em tabelas
SELECT <nome_coluna>
FROM <nome_tabela> AS <outro_nome_tabela>