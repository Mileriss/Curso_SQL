/*
LEFT JOIN - Vai retornar todos os registros que estiverem na tabela esquerda (tabela1) e todos os
registros que tiverem relacionados na tabela da direita (tabela2)
*/

#Utilizando o LEFT JOIN
SELECT <coluna>
FROM <tabela1>
LEFT JOIN <tabela2>
ON <tabela1.coluna> = <tabela2.coluna>

